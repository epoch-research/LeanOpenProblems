# Attempted-list names excluded from the eval task (3 of 353).
#
# erdos_677_stronger and erdos_7.variant.autoformalized have never existed in
# the public FC repository: a full-history `git log -S <name> --all` finds
# nothing, and the live `main` (checked 2026-07-27, including renamed
# `variants.*` forms) has no such statements -- presumably attempted against a
# Google-internal FC state that was never upstreamed.
#
# erdos_729 exists on FC `main` only *after* the vendored commit 67338a1
# (upstreamed ~July 2026). Including it would need special-cased statement
# adaptation against a different FC state than everything else; provenance
# purity won over the +1 sample. Revisit if the sandbox images ever move past
# its upstreaming.
erdos_677_stronger
erdos_7.variant.autoformalized
erdos_729
